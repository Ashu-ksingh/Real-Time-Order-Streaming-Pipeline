# kafka_config.py — Configuration for Kafka connections and topic setup

KAFKA_CONFIG = {
    "bootstrap_servers": "localhost:9092",
    "topic": "orders",
    "group_id": "spark-streaming-consumer"
}
